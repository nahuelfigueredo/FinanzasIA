<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid brand-row">
        <a class="navbar-brand" href="">
            <img class="brand-icon" src="brand-icon.svg" alt="" aria-hidden="true" />
            <span>Finanzas IA</span>
        </a>
        <div class="brand-actions">
            <NotificationsBell />
            <ThemeToggle />
        </div>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Inicio
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="movimientos">
                <span class="bi bi-cash-stack-nav-menu" aria-hidden="true"></span> Movimientos
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="presupuesto">
                <span class="bi bi-bullseye-nav-menu" aria-hidden="true"></span> Presupuesto
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <details class="nav-submenu" onclick="event.stopPropagation()">
                <summary class="nav-link">
                    <span class="bi bi-calendar-report-nav-menu" aria-hidden="true"></span> Reportes
                </summary>
                <div class="nav-submenu-items">
                    <NavLink class="nav-link nav-sublink" href="reporte-mensual">Mensual</NavLink>
                    <NavLink class="nav-link nav-sublink" href="reportes/semanal">Semanal</NavLink>
                    <NavLink class="nav-link nav-sublink" href="reportes/anual">Anual</NavLink>
                    <NavLink class="nav-link nav-sublink" href="reportes/por-categorias">Por categorías</NavLink>
                    <NavLink class="nav-link nav-sublink" href="reportes/por-cuentas">Por cuentas</NavLink>
                    <NavLink class="nav-link nav-sublink" href="reportes/comparativas">Comparativas</NavLink>
                    <NavLink class="nav-link nav-sublink" href="reportes/exportar-pdf">Exportar PDF</NavLink>
                    <NavLink class="nav-link nav-sublink" href="reportes/exportar-excel">Exportar Excel</NavLink>
                </div>
            </details>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="finanzas-ia">
                <span class="bi bi-chat-dots-nav-menu" aria-hidden="true"></span> Finanzas IA
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <details class="nav-submenu" onclick="event.stopPropagation()">
                <summary class="nav-link">
                    <span class="bi bi-gear-nav-menu" aria-hidden="true"></span> Configuración
                </summary>
                <div class="nav-submenu-items">
                    <NavLink class="nav-link nav-sublink" href="configuracion">Preferencias</NavLink>
                    <NavLink class="nav-link nav-sublink" href="categorias">Categorías</NavLink>
                    <NavLink class="nav-link nav-sublink" href="cuentas">Cuentas</NavLink>
                </div>
            </details>
        </div>
    </nav>
</div>

<AuthorizeView>
    <Authorized>
        <div class="nav-user-footer">
            <div class="nav-user-card">
                <div class="nav-user-avatar" aria-hidden="true">
                    @GetInitials(context.User.Identity?.Name)
                </div>
                <div class="nav-user-info">
                    <span class="nav-user-name">@GetDisplayName(context.User.Identity?.Name)</span>
                    <span class="nav-user-email">@(context.User.FindFirst(System.Security.Claims.ClaimTypes.Email)?.Value ?? context.User.Identity?.Name)</span>
                </div>
                <form action="/logout" method="post" class="nav-logout-form">
                    <AntiforgeryToken />
                    <button type="submit" class="nav-logout-btn" title="Cerrar sesión">
                        <span class="bi bi-arrow-bar-left" aria-hidden="true"></span>
                    </button>
                </form>
            </div>
        </div>
    </Authorized>
</AuthorizeView>

@code {
    private static string GetInitials(string? name)
    {
        if (string.IsNullOrWhiteSpace(name)) return "?";
        var visible = name.Contains('@') ? name[..name.IndexOf('@')] : name;
        var parts = visible.Split([' ', '.', '_', '-'], StringSplitOptions.RemoveEmptyEntries);
        return parts.Length switch
        {
            0 => "?",
            1 => char.ToUpperInvariant(parts[0][0]).ToString(),
            _ => $"{char.ToUpperInvariant(parts[0][0])}{char.ToUpperInvariant(parts[1][0])}"
        };
    }

    private static string GetDisplayName(string? name)
    {
        if (string.IsNullOrWhiteSpace(name)) return "Usuario";
        return name.Contains('@') ? name[..name.IndexOf('@')] : name;
    }
}
